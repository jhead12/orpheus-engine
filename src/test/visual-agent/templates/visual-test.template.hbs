```handlebars
// Handlebars template for visual test generation
// This file is NOT a TypeScript file and should be excluded from compilation
// Usage: processed by the template engine

import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import {{ComponentName}} from '{{ComponentPath}}';

describe('{{testNamePattern}}', () => {
  it('renders correctly', () => {
    const { container } = render(<{{ComponentName}} {{#each props}}{{@key}}={{{this}}} {{/each}}/>);
    expect(container).toMatchSnapshot();
  });
});
```
