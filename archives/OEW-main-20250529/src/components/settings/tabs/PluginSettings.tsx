import React from 'react';
import { Typography } from '@mui/material';

const PluginSettings: React.FC = () => {
  return (
    <div>
      <Typography variant="h6" gutterBottom>Plugin Configuration</Typography>
      {/* TODO: Implement plugin settings */}
      <Typography>Plugin settings configuration will be available in a future update.</Typography>
    </div>
  );
};

export default PluginSettings;
